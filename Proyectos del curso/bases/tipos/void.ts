// que es un void?
// El tipo void se utiliza para indicar que una función no devuelve ningún valor.
function callBatman (): void {
    return ;

}
 //diferencia entre una funcion y una funcion flecha?
 // Una función normal puede ser declarada con la palabra clave function y puede tener un nombre. 
// Una función flecha es una forma más concisa de escribir funciones en JavaScript y TypeScript, utilizando la sintaxis de flecha (=>).
const callSuperman = (): void => {
    return
}
const a = callBatman();
console.log(a); 